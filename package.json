{
  "name": "droi-game-tool",
  "private": true,
  "scripts": {
    "deploy:ronin": "cd contracts && npm run deploy:ronin",
    "dev:api": "uvicorn backend.app.main:app --reload --port 8000",
    "dev:worker": "rq worker pixelwork --url redis://localhost:6379/0",
    "dev:frontend": "cd frontend && npm run dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.71.0"
  }
}
